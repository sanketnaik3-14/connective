name: connective
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Using a stable, older set of packages for compatibility
  riverpod: ^2.6.1
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1

  firebase_core: ^4.0.0
  firebase_auth: ^6.0.0
  cloud_firestore: ^6.0.0
  firebase_storage: ^13.0.0
  cloud_functions: ^6.0.0


  # Reverting google_sign_in to a stable version
  google_sign_in: ^6.2.0

  # Other Packages
  go_router: ^16.1.0
  image_picker: ^1.1.2
  font_awesome_flutter: ^10.7.0
  intl: ^0.20.2
  google_fonts: ^6.2.1
  heroicons: ^0.11.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  build_runner: ^2.4.11
  riverpod_generator: ^2.6.1
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true